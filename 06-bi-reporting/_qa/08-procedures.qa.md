# Q&A Log — Procedures (SOPs)
# Append-only. Do not edit past entries; add new dated entries instead.

## 2026-09-22 — Q6.6: Domain-specific procedures

**Bucket:** [Standard]/AI-draftable — each procedure executes an already-
decided Policy/Access Rule pair.

**Answer:** 3 procedures. BIPROC-2 (Row-Level Security validation) is
deliberately nested inside BIPROC-1's certification-review flow rather
than standing fully independent, mirroring how System Architecture's
SAPROC-5 nested inside its own certification-style flow.

**Status:** Pending explicit review.
