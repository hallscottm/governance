# Term Linking Convention (DRY / anti-drift rule)

Status: Draft
Ratified: No
Last updated: 2026-09-22

Applies to every domain's Definitions (and later, Taxonomy/Ontology) files
across the whole framework.

## Rule

A term is written exactly once, in its owner domain's definitions file.
No other file ever copies its text. Anything elsewhere that needs the term
holds a link, never a duplicate definition.

## Mechanics

1. **Stable ID** — every term gets a slug of the form `<domain-prefix>-<kebab-case-term>`,
   e.g. `infra-gpu-memory`. IDs are permanent even if display wording changes.

2. **Anchor heading** — the term is defined once, under a heading carrying
   its ID as an anchor:
   ```markdown
   ### GPU Memory (VRAM) {#infra-gpu-memory}
   Dedicated high-bandwidth memory on a GPU...
   Source: Common industry practice
   Referenced by: System Architecture, Harness
   ```

3. **Reference stub** — a domain that needs a term it doesn't own adds a
   pointer, never a redefinition, typically in a "Referenced Terms (owned
   elsewhere)" section of its own Definitions file:
   ```markdown
   - **GPU Memory (VRAM)** — owned by Infrastructure →
     [01-infrastructure/01-definitions.md#infra-gpu-memory](../01-infrastructure/01-definitions.md#infra-gpu-memory)
   ```

4. **Reverse tag** — the owner's own entry carries a `Referenced by:` line
   naming which other domains point to it, kept in sync manually for now
   (candidate for automation once a generation script exists).

5. **Unified glossary** — `00-framework/glossary-master.md` is generated by
   scanning every domain's Definitions file for `{#id}` anchors and compiling
   one index of links back to each term's single owning file. It never
   contains copied definition text.

## Domain prefixes

| Domain | Prefix |
|---|---|
| Infrastructure | `infra` |
| Networking | `net` |
| System Architecture | `sysarch` |
| Data Platform | `dp` |
| Data/Metadata | `data` |
| Business Intelligence / Reporting | `bi` |
| Workflow/Process | `wf` |
| Harness | `harness` |
| Cross-Cutting: Roles & Departments (roles) | `ccrole` |
| Cross-Cutting: Roles & Departments (departments) | `ccdept` |
| Cross-Cutting: Roles & Departments (governance bodies) | `ccgov` |
| Cross-Cutting: Roles & Departments (org/reporting/training mechanisms) | `ccorg` |
| Cross-Cutting: standalone pillars (e.g. Sandbox) not tied to a role/department/domain | `cc` |
| Engagements — top-level area, not a domain, sits above the domain axis | `eng` |

`bi` added 2026-09-22 when Business Intelligence / Reporting was split
out as its own domain (see 00-framework/ea-framework-alignment.md) —
covers report-level artifacts (Report Data Model, Calculated Field,
source-to-target mapping) that sit downstream of, and can diverge from,
Data Platform's official Semantic Layer and Data/Metadata's official
Metric definitions.

`dp` added 2026-09-22 when Data Platform was split out of System
Architecture as its own domain (see 00-framework/ea-framework-alignment.md).
Kept distinct from `data` (Data/Metadata) on purpose: `dp` owns where data
physically lives and runs (databases, lakes, lakehouses, warehouses,
vector stores, streaming); `data` owns what it means (classification,
stewardship, catalog, governance).

`iface` (Interface/Human) removed 2026-09-22 — that domain was folded
into the Roles & Departments cross-cutting pillar (`ccorg` prefix; see
cross-cutting/roles-and-departments/00-qa.md Q11). It had no drafted
content, so no anchors needed migrating.

`eng` added 2026-09-23 for the Engagements area (engagements/) — the unit-of-work planning layer (Engagement Documents, Vetting/Planning Agents) that sits above the domain axis and consumes it, rather than adding a ninth technology-layer row. Not cross-cutting either (it isn't expressed through every domain's Policies/Access Rules columns) — its own top-level area, alongside 00-framework/ and cross-cutting/. See 00-framework/domain-axis-definition.md's "Areas outside the domain axis" note and engagements/01-definitions.md.

Cross-cutting prefixes follow the same anchor/reference-stub/reverse-tag
mechanics as domain prefixes — a role or department is defined once in
cross-cutting/roles-and-departments/01-definitions.md, and every domain's
Access Rules column links to it rather than restating it. Added
2026-09-22 when Roles & Departments was established as a third
cross-cutting pillar alongside Governance and Security.

`ccorg` added 2026-09-22 alongside the Interface/Human fold-in above —
covers the Org Chart / Reporting Line / Training Requirement mechanisms
that moved into Roles & Departments rather than the roles (`ccrole`),
departments (`ccdept`), or governance-body (`ccgov`) registries proper.

`cc` added 2026-09-22 for cross-cutting concepts that aren't a role,
department, governance body, or org mechanism at all — the first use is
Sandbox (`cc-sandbox`, cross-cutting/sandbox-activation.md), one of this
framework's three cross-cutting pillars (Governance/Security, Roles &
Departments, Sandbox) rather than a domain row. Reserved for genuinely
standalone cross-cutting terms; a term that fits an existing cross-cutting
registry (role, department, governance body, org mechanism) still uses
that registry's prefix instead.

## Inheritance (`extends`) — for genuine specialization, not homonyms

Reference stubs (above) handle *same word, unrelated meanings* across
domains — e.g. two terms that happen to share a name but describe different
things, kept fully independent under different IDs.

Inheritance is a different, narrower relationship: *same underlying
concept, one domain narrows or adds detail to it*. Use `extends` only when
the child's meaning would never contradict the parent — if it would, it's
not a specialization, it's an independent term (use a reference stub or a
standalone definition instead).

### Rules

1. **One level only.** A term may extend exactly one parent. No chains
   (`A extends B extends C`). Keeps resolution simple and matches the
   quality bar (00-framework/quality-bar.md).
2. **The child writes only the delta** — what it adds or narrows — never
   a restatement of the parent's full text.
3. **Format:**
   ```markdown
   ### Infrastructure Risk {#infra-risk}
   Extends: governance-risk
   Adds: risk specifically arising from hardware failure, capacity
   exhaustion, or facility-level disruption.
   Source: Org convention, building on NIST SP 800-53 (RA family)
   ```

## Authoring format vs. AI-consumption format

The source files (as described above) are optimized for humans
maintaining the framework: DRY, no duplication, no drift, one place to
edit. That is the right shape for authoring, but it is not the right
shape to hand an AI/harness at runtime — resolving `extends` and
`Referenced by` links correctly, every time, across every query, is a
multi-hop task an AI can get wrong silently (a missed hop reads as a
confident but incomplete answer).

**Rule:** a second, generated artifact —
`00-framework/ai-resolved-glossary.md` — holds the fully flattened,
self-contained version of every term: parent text merged with every
child's delta, in one place, with a provenance note on which part came
from where. This is the artifact an AI/harness/retrieval system should
actually be pointed at, never the raw source files with unresolved links.

Same principle as `glossary-master.md` (generated, never hand-edited) —
this is the AI-consumption-optimized sibling of that index. Not built yet;
build it the first time a real `extends` case exists to resolve.
