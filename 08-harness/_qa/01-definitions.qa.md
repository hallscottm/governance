# Q&A Log — Definitions
# Append-only. Do not edit past entries; add new dated entries instead.

## 2026-09-23 — Q8.2: Subdomain structure and term list confirmed with user before drafting

**Bucket:** [Judgment]/User-confirmed — user explicitly approved the
5-subdomain, ~22-term plan (Agent Definition & Scope; Prompting & Context
Management; Guardrails & Safety Controls; Observability & Evaluation;
Escalation & Human-in-the-Loop) before any content was written, same
checkpoint pattern used for Workflow/Process.

**Answer:** 24 terms drafted across the 5 confirmed subdomains. Two
name-collision boundaries stated explicitly rather than left implicit:
Agent (`harness-agent`, an AI system) vs. Workflow/Process's CI
Runner/Agent (`wf-ci-runner`, compute executing a pipeline stage); and
Behavioral Drift (`harness-behavioral-drift`, a model/agent concept) vs.
Workflow/Process's Drift Detection (`wf-drift-detection`, an
infrastructure-state concept). Deliberately does *not* redefine
`ccrole-ai-agent-harness` (Roles & Departments) — that term stays the
org-chart/permission classification; this domain owns the technical
configuration record.

**Applied to:** 01-definitions.md.

**Status:** Pending explicit review.

## 2026-09-23 — Q8.3: Sandbox's role in Harness

**Bucket:** [Standard]/AI-draftable, following through on
cross-cutting/sandbox-activation.md's own Primary Uses section (drafted
immediately before this domain, in the same session).

**Answer:** Sandbox referenced as an owned-elsewhere term rather than
redefined; an Agent's Capability Scope and Guardrails are tested in a
Sandbox before being granted against Production, per that file's own
stated Primary Uses.

**Applied to:** 01-definitions.md's Referenced Terms section.

**Status:** Pending explicit review.
