# Q&A Log — Conventions (Data/Metadata)
# Append-only. Do not edit past entries; add new dated entries instead.

## 2026-09-22 — Q5.1: Naming conventions

**Bucket:** [Standard]/AI-draftable — reused System Architecture's
stable-name/env-as-metadata decision rather than re-litigating it, since
the same reasoning applies identically to Data Assets.

**Drafted:** Data Asset/Dataset naming (`<domain>-<asset-name>`,
kebab-case, no env segment); Data Domain naming (Title Case, free-text,
deliberately non-kebab since it's a business concept, not an
identifier); Business Glossary Term naming (natural business language,
no enforced casing — explicit departure, flagged); Sensitivity Level tag
convention (kebab-case, four fixed values).

**Status:** Pending explicit review.
